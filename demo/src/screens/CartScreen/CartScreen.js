import React from 'react';

/**
 * @return {!JSX} Page component for where a user can review
 *     the products in their shopping cart.
 */
export function CartScreen() {
  return (
    <div>
      <h1>{`I'm the Shopping Cart Page!`}</h1>
    </div>
  );
}
