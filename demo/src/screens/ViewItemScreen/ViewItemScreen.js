import React from 'react';

/**
 * Component to display a single item
 * @return {!JSX} Returns a component to view an item.
 */
export function ViewItemScreen() {
  return (
    <div>
      <h1>{`I'm the View item  Page!`}</h1>
    </div>
  );
}
